<UserControl x:Class="CorporateSubscriptionManager.Views.RequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="350" d:DesignWidth="400">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Выберите сервис:"/>
        <ComboBox Grid.Row="0" Margin="120,0,0,6" ItemsSource="{Binding Services}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedService}"/>

        <Label Grid.Row="1" Content="Обоснование:"/>
        <TextBox Grid.Row="1" Margin="120,0,0,6" Text="{Binding Comment}" AcceptsReturn="True" Height="80"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,6">
            <Button Content="Отправить" Command="{Binding SubmitCommand}" Margin="0,0,10,0"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}"/>
        </StackPanel>

        <Separator Grid.Row="3" Margin="0,6"/>

        <!-- Список карточек заявок -->
        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Requests}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8" Background="White">
                            <StackPanel>
                                <TextBlock Text="{Binding RequestDate, StringFormat='Дата: {0:dd.MM.yyyy HH:mm}'}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ServiceName}" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding Status}" Foreground="Gray" Margin="0,4,0,0"/>
                                <!-- Показываем кто апрувнул (временное поле для проверки) -->
                                <TextBlock Text="{Binding ApproverID, StringFormat='Одобрил (ID): {0}'}" Margin="0,6,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>