<Window x:Class="CorporateSubscriptionManager.Views.ApproveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Date" Height="140" Width="320" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="End date:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="StartDatePicker" SelectedDate="{Binding SelectedStartDate, RelativeSource={RelativeSource AncestorType=Window}}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="80" Margin="0,0,8,0" Click="Ok_Click">OK</Button>
            <Button Width="80" Click="Cancel_Click">Cancell</Button>
        </StackPanel>
    </Grid>
</Window>